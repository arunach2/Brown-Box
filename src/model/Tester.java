package model;

import java.util.Scanner;

import view.AccessingExistingMember;
import view.SigningUpMember;

public class Tester {
	public static void main(String[] Args) {
		// Movie movie = MovieBuilder.build("Batman");
		// System.out.println(movie.getCost() + " " + movie.getDirector() + " " + movie.getCost());
		// Member member = MemberBuilder.access("arunach2", "Hog123er");
		// System.out.println(member.getFirstName() + " " + member.getLastName());
		// ListMovies.access();
		/*ShoppingCart shoppingCart = new ShoppingCart();
		ListCategoricalMovies.access(3, shoppingCart);
		shoppingCart.printItems(); */
		// SigningUpMember.initiate();
		
	}
}
